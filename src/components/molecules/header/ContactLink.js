import React from "react";
import Button from "../../atoms/header/Button";

const ContactLink = () => {
  return (
    <div className="contact">
      <Button href="#Kontak">Contact</Button>
    </div>
  );
};

export default ContactLink;
